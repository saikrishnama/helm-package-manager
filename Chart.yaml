apiVersion: v2
name: my-app
version: 1.0.1
dependencies:
  - name: redis
    version: 20.11.3  # Updated to a valid version for HTTPS
    repository: https://charts.bitnami.com/bitnami  # Switched to HTTPS
    condition: redis.enabled
  - name: postgresql
    version: 16.5.5  # Updated to a valid version for HTTPS
    repository: https://charts.bitnami.com/bitnami  # Switched to HTTPS
    condition: postgresql.enabled