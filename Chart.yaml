apiVersion: v2
name: my-app
version: 1.0.0
dependencies:
  - name: redis
    version: 17.3.5  # Updated to a valid version for HTTPS
    repository: https://charts.bitnami.com/bitnami  # Switched to HTTPS
    condition: redis.enabled
  - name: postgresql
    version: 13.3.0  # Updated to a valid version for HTTPS
    repository: https://charts.bitnami.com/bitnami  # Switched to HTTPS
    condition: postgresql.enabled